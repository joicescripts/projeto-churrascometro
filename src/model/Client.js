export default class Client {
    constructor(nome, email, postalCode, optIn) {
        this.nome = nome;
        this.email = email;
        this.postalCode = postalCode;
        this.optIn = optIn;
    }
}
